# DeepSeek 执行规则

你正在维护一个个人使用的 Sitemap 新页面关键词发现器。

## 不可擅自扩展

- 不做网页后台。
- 不做登录、多用户、收费系统。
- 不接搜索量付费接口。
- 不自动注册域名。
- 不监控删除页面和页面正文变化。
- 不引入数据库，首版只使用 Cloudflare KV。
- 不把飞书 Webhook、手动执行密钥写进代码或提交到 Git。

## 修改规则

1. 每次只完成一个明确任务。
2. 修改前先阅读相关文件和测试。
3. 修改后运行 `npm run check`。
4. 检查失败必须修复，不允许只解释。
5. 不重构与当前任务无关的文件。
6. 不删除已有测试；发现缺陷时补测试。
7. 输出时只报告：改了什么、测试结果、剩余风险。

## 完成标准

- TypeScript 严格类型检查通过。
- 单元测试通过。
- Wrangler 干运行构建通过。
- 首次运行只建立基线，不把全部旧网址当成新增。
- 飞书发送失败时不覆盖旧快照，确保下次重试。
